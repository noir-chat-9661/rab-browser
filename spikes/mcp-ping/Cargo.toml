[package]
name = "mcp-ping-spike"
version = "0.1.0"
edition.workspace = true
publish = false

[dependencies]
anyhow = "1"
rmcp = { version = "3.4.0", features = ["macros", "server", "transport-io"] }
tokio = { version = "1", features = ["rt-multi-thread", "macros"] }

